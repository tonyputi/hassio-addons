configuration:
  provider:
    name: "AI Provider"
    description: "The AI provider to use (e.g. gemini, openai, anthropic). Used only for the initial setup — change it later via the Web UI terminal."
  api_key:
    name: "API Key"
    description: "Your API key for the chosen provider. Used only for the initial setup."
